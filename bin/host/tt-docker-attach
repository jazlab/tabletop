#!/usr/bin/env bash

set -e

bin_dir=$(dirname $(realpath ${BASH_SOURCE[0]}))
source $bin_dir/../../setup.bash

cd $TABLETOP_DIR

if [[ $TERM = xterm-kitty ]]; then
    CMD="kitten run-shell --shell=bash"
else
    CMD=bash
fi

docker exec -it tabletop-server-dev-1 $CMD

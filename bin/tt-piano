#!/usr/bin/env bash

bin_dir=$(dirname $(realpath ${BASH_SOURCE[0]}))
TABLETOP_DIR=$(realpath $bin_dir/..)

set -e

pushd $TABLETOP_DIR
source venv/bin/activate
python src/tabletop_server/tabletop_server/flic_client/piano.py "$@"
popd
